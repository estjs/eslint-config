{
  "name": "@estjs/eslint-config-monorepo",
  "version": "0.2.8",
  "private": true,
  "license": "MIT",
  "author": "estjs",
  "scripts": {
    "lint": "eslint . --ext .js,.ts,.json,.vue,.md --fix",
    "release": "bumpp package.json packages/*/package.json --commit --push --tag && pnpm -r publish --access public"
  },
  "devDependencies": {
    "@estjs/eslint-config": "*",
    "bumpp": "^7.1.1",
    "eslint": "^8.13.0",
    "typescript": "^4.6.3"
  }
}

{
	"name": "@estjs/eslint-config-monorepo",
	"version": "0.9.1-beta.4",
	"private": true,
	"author": "estjs",
	"license": "MIT",
	"scripts": {
		"build": "pnpm -r build",
		"test": "pnpm -r test",
		"clean": "rimraf ./packages/**/dist & rimraf ./packages/**/node_modules rimraf node_modules rimraf pnpm-lock.yaml",
		"lint": "eslint . --ext .js,.ts,.json,.vue,.md --fix",
		"release": "pnpm -r run build && bumpp package.json packages/*/package.json --no-git-checks --commit --push --tag && pnpm -r publish --access public"
	},
	"devDependencies": {
		"@estjs/eslint-config": "*",
		"bumpp": "^9.2.0",
		"eslint": "^8.54.0",
		"prettier": "^3.1.0",
		"rimraf": "^5.0.5",
		"typescript": "^5.3.2"
	}
}

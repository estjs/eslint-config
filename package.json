{
  "name": "@estjs/eslint-config",
  "version": "0.2.2",
  "license": "MIT",
  "author": "estjs",
  "scripts": {
    "lint": "eslint . --ext .js,.ts,.json,.vue,.md --fix",
    "release": "bumpp package.json packages/*/package.json --commit --push --tag && pnpm -r publish --access public"
  },
  "devDependencies": {
    "@estjs/eslint-config": "*",
    "eslint": "^8.13.0",
    "bumpp": "^7.1.1",
    "typescript": "^4.6.3"
  }
}
